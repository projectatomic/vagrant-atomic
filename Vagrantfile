# BEING USED FOR TESTING, REMOVE BEFORE RELEASE
#
# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure('2') do |config|
  config.vm.box = 'rhel-atomic-7.0'
  config.vm.synced_folder './', '/vagrant', type: 'rsync'
end

